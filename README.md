# 求职投递助手

这是原有投递系统与可靠候选人档案内核整合后的本地 Web 应用。

正式入口仍保留原系统已有的 Agent API 配置、公司与岗位搜索、岗位推荐、简历润色、人工消息干预、运行监控、通知和记忆能力。新的加密 PDF 与候选人档案版本系统替换了旧的“个人信息 JSON + 最新上传简历 + `resume_text_cache.json`”简历来源。

腾讯文档投递按产品范围决定移除，不属于当前最小跑通路径。

## 本版本更新

当前整合版的更新、修复和已知限制见 [2026-07-31 更新说明](docs/UPDATE_2026-07-31.md)。

维护者：by yjc

## 现在的主流程

1. 在“Agent 连接”中填写并验证原有模型 API。
2. 在“文件管理”上传 PDF 简历。PDF 与当次提取正文、证据会分别加密保存；后续润色复用提取记录，不再重复解析 PDF，也不再依赖明文“最新简历”。
3. 核对 PDF 提取字段后建立候选人档案。未经确认的提取结果不会直接用于投递。
4. 后续上传新 PDF 时，自主勾选要增量更新的字段。确认后建立独立版本，不覆盖旧版本。
5. 可以编辑现有档案并另建版本，也可以切换或删除未被申请引用的旧版本。
6. 回到“投递任务”，输入公司、岗位方向、城市和可选招聘链接，启动原有搜索、推荐、润色与填表流程。本次会话会固定绑定当前档案版本。
7. 系统弹出受管 Chromium 后，只在这个窗口登录或注册。不要把网址复制到其他浏览器；其他浏览器不共享这个自动化会话的登录状态。
8. 系统填完表单后停在最终提交前。由用户在受管窗口中检查并亲自点击最终提交，Agent 不代点不可逆按钮。
9. 飞书招聘只有出现可验证回执才记为成功；用户已点击但没有观察到回执时，明确记录为结果未知。

人工接管期间只会收集不含输入值的字段定位候选。候选规则需要审核批准后才能复用，不会把密码、验证码或表单内容拿去自动训练。

## 安装

要求：Windows、Python 3.12。

```powershell
cd E:\yujch\Personal\app_trial\findjob\find_job_agent
python -m venv .venv
.\.venv\Scripts\python.exe -m pip install -e .
.\.venv\Scripts\python.exe -m playwright install chromium
```

默认数据目录为 `data/core`：

- SQLite 保存候选人档案版本及可靠状态；
- PDF 文件使用内容寻址和 AES-GCM 加密；
- PDF 提取正文、页码、坐标、方式和置信度以加密提取记录保存，并按文件资源复用；
- 主密钥由 Windows DPAPI 保护；
- `browser-profile` 保存受管浏览器登录状态，但应用不保存账号密码。

## 一键启动

双击项目根目录的 `start_findjob.bat`。脚本只做三件事：进入项目目录、设置本地 `PYTHONPATH`、用项目 `.venv` 启动 Uvicorn。它不会下载文件、修改安全设置、自动打开浏览器或执行 PowerShell 脚本。

随后手动打开：

<http://127.0.0.1:8000/>

API 文档位于：

<http://127.0.0.1:8000/docs>

不要把监听地址改成公网 IP。候选人档案、简历和浏览器登录状态都属于敏感本地数据。

## 你现在应该怎样实际验证

不需要先跑完整自动化测试。直接按以下顺序完成一次人工流程：

1. 双击 `start_findjob.bat`，确认终端没有 traceback，访问首页能看到原投递系统，而不是被化简后的独立任务页。
2. 打开“Agent 连接”，填写 API 地址、密钥和模型名并验证连接。
3. 打开“文件管理”，上传一份真实 PDF 简历，核对提取字段，点击建立候选人档案。
4. 刷新档案列表，确认出现“版本 1（当前投递版本）”；尝试“编辑并新建版本”，确认旧版本仍存在且能够切换。
5. 返回“投递任务”，先只配置一家公司和一个真实可访问的招聘链接，不要并行投递。
6. 启动后观察原有搜索、推荐、岗位选择和润色审核是否正常出现。
7. 到登录阶段，只操作系统弹出的受管浏览器窗口；完成登录后回 WebUI 点击“已完成登录”。
8. 检查自动填写内容与上传的 PDF。第一次可以停在最终提交前，以验证 Agent 不会自动点击提交。
9. 只有在你确实愿意投递该职位时，才亲自在受管浏览器中点击最终提交，然后回 WebUI 确认结果。
10. 若任何一步失败，保留启动窗口里的完整 traceback、WebUI 提示、当前阶段和招聘页面网址；不要只截最后一句错误。

## 架构边界

- 候选人档案版本是投递事实的唯一来源；Agent 记忆只能补充交互字段，不能覆盖档案。
- 每次投递会话绑定一个明确版本，后续档案更新不会改变已启动会话。
- 浏览器上传要求文件路径，因此会话期间会把已绑定的加密 PDF 临时解密到系统临时目录；取消会话或关闭服务时清理。
- 原模型 API 和 Agent 编排继续负责搜索、推荐、润色与受控填表，但不再负责长期保存简历事实。
- React 任务页保留在 `/app` 作为辅助诊断界面，正式入口是 `/` 的统一原系统。

架构术语见 `CONTEXT.md`，决策记录见 `docs/adr/0043-integrate-controlled-core-into-original-delivery-system.md`。
