# 使用 SQLite 和内容寻址文件资源

候选人档案、版本关系、职位申请、状态变化、材料选择和接管轨迹元数据使用 SQLite 作为事务性权威存储；PDF、DOCX、截图等文件按内容哈希保存为不可变文件资源，并由数据库通过稳定资源标识引用。相同内容自动去重，每个申请显式绑定具体档案版本和材料版本，不再根据路径、修改时间或“最新上传”选择数据。JSON 仅用于导入、导出和诊断，不再承担权威持久化。
