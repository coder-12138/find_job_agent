# 使用 React、TypeScript 和 Vite 重建任务前端

新任务式界面使用 React、TypeScript 和 Vite 构建，按候选人档案、职位申请、浏览器任务和审计记录拆分模块，并通过类型化客户端调用显式命令查询 API。构建产物继续由 FastAPI 同源提供，不增加独立前端服务、跨域部署或第二套运行时配置。

旧版单体 HTML、原生 JavaScript 和 CSS 页面停止承载新功能，只允许修复会阻塞迁移或危及现有数据的缺陷。新任务前端可以在开发期间通过受控开关验证，但首个垂直切片验收后必须替换原正式入口，不长期向求职者暴露两套界面。
