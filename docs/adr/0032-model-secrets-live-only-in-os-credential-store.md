# 模型密钥只保存在操作系统凭据库

模型服务地址、模型名称和任务能力等非秘密模型配置可以持久化，API 密钥只能由后端写入操作系统凭据库，不得进入浏览器存储、SQLite、环境文件、日志或 API 响应。Web UI 只在新增或替换密钥时传递一次，删除模型配置时同步删除系统凭据；产品同时支持不需要密钥的本地模型端点。模型按字段映射、职位分析和表达建议等有界任务调用，不再绑定聊天会话或控制投递状态。
