# 使用显式命令查询 API，实时连接只传递进度通知

任务式界面通过查询 API 读取候选人档案、职位申请、当前状态和审计事件，通过领域命令请求创建版本、切换版本、确认填表和浏览器操作等业务变化。会改变状态的命令必须携带幂等标识；存在并发覆盖风险时，还必须携带调用方读取到的预期版本，由服务端进行冲突检查。

WebSocket 或 SSE 只传递可以丢失和重建的进度通知，不承载唯一业务事实、人工决定或操作成功证据。连接断开、页面刷新或服务重启后，客户端必须从 SQLite 权威状态重新查询并恢复界面，不得通过重放聊天消息或内存会话恢复工作流。
