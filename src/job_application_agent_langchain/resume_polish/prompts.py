"""简历润色模块的提示词模板。

所有提示词使用中文，要求 LLM 返回严格的 JSON（不包含 markdown 代码块或额外解释）。
"""

# ----------------------------------------------------------------------------
# JD 分析提示词：从岗位 JD 中提取关键要求
# ----------------------------------------------------------------------------
JD_ANALYSIS_PROMPT = """你是一位资深的招聘分析师和技术招聘专家。请仔细分析下面的岗位 JD（职位描述），提取关键信息。

## 待分析的 JD：
{jd}

## 任务
请从 JD 中提取以下信息，并以严格的 JSON 格式返回（不要包含任何额外文字、不要使用 markdown 代码块）：

{{
  "required_skills": ["JD 中明确要求的必备技能/能力列表"],
  "preferred_skills": ["JD 中提到的加分项/优先（nice-to-have）技能列表"],
  "experience_requirements": "对该岗位的经验要求描述（如：3年以上后端开发经验、应届生、有大规模系统经验等）",
  "keywords": ["JD 中的重要关键词/术语（技术栈、业务领域、方法论等）"],
  "responsibilities": ["该岗位的核心职责列表"],
  "summary": "对该岗位所需候选人画像的简要总结（1-2句话）"
}}

## 要求
- required_skills 与 preferred_skills 应区分「明确要求」与「加分项」
- keywords 应包含可在简历中匹配的具体技术/业务术语
- 全部使用中文（除非是专有技术名词，如 Python、Kubernetes 等）
- 只返回 JSON，不要任何解释
"""


# ----------------------------------------------------------------------------
# 简历润色提示词：基于 JD 分析与候选人真实经历，生成针对性的润色内容
# ----------------------------------------------------------------------------
RESUME_POLISH_PROMPT = """你是一位专业的简历顾问，擅长根据目标岗位 JD 优化求职者的简历表达，使其更契合岗位需求、更具竞争力。

## 目标岗位 JD 分析结果：
{jd_analysis}

## 候选人的完整简历信息：
{user_summary}

## 与 JD 高度相关的候选内容（程序化匹配结果，供你参考重点突出）：
{relevant_content}

## 你的任务
基于候选人的【真实简历内容】，针对该岗位 JD 进行自适应润色。请严格遵守以下原则：
1. **绝不编造**：只能改写、重新组织、强调候选人已有的真实经历，不得捏造任何不存在的项目、技能、数据或成果。
2. **针对性改写自我介绍**：撰写一段突出候选人背景与该岗位匹配度的自我介绍，自然融入 JD 中的关键术语。
3. **重排并强化项目经历**：将与 JD 相关的项目放在前面，在描述中自然融入 JD 关键词，尽量量化成果（仅在原始数据存在时）。
4. **突出匹配技能**：重点展示与 JD required_skills/preferred_skills 匹配的技能，并说明其与岗位的相关性。
5. **强化工作/实习经历**：针对与岗位相关的工作经历，调整描述以呼应岗位职责。
6. **用词专业**：使用 JD 中的术语和行业惯用表达，提升专业感。
7. **整体契合度总结**：给出一段候选人整体匹配该岗位的总结。

## 输出格式
请返回严格的 JSON（不要包含任何额外文字、不要使用 markdown 代码块），结构如下：

{{
  "self_introduction": "针对该岗位润色后的自我介绍（2-4句话）",
  "project_highlights": [
    {{
      "name": "项目名称",
      "role": "担任角色",
      "description": "针对岗位润色后的项目描述（融入 JD 关键词，量化成果）",
      "relevance_to_jd": "该项目与岗位的相关性说明"
    }}
  ],
  "skill_highlights": [
    {{
      "skill": "技能名称",
      "level": "掌握程度",
      "relevance": "该技能与岗位的相关性"
    }}
  ],
  "work_highlights": [
    {{
      "company": "公司名称",
      "position": "职位",
      "description": "针对岗位润色后的工作描述",
      "relevance": "该经历与岗位的相关性"
    }}
  ],
  "summary": "候选人整体匹配该岗位的总结（2-3句话）"
}}

## 重要提醒
- project_highlights / skill_highlights / work_highlights 中的条目必须来源于候选人真实简历，不得新增。
- 若候选人某类经历为空，对应列表返回空数组 []。
- 顺序应按与岗位的相关性从高到低排列。
- 只返回 JSON，不要任何解释。
"""
